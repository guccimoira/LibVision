<!-- res/drawable/adminbutton_background.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/> <!-- White background -->
    <corners android:radius="8dp"/> <!-- Optional: set corner radius -->
    <stroke android:width="1dp" android:color="@android:color/darker_gray"/> <!-- Optional: add border -->
</shape>
